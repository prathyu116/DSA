
  function kadane(arr){
    var maxi=-999999999999999
    var sum=0;
    for(var i=0;i<arr.length;i++){
      sum=sum+arr[i]
      maxi=Math.max(sum,maxi)
      if(sum<0) sum=0
    }
console.log(maxi)
  }
  var arr=[-2,1,-3,4,-1,-2,1,-5,4]
kadane(arr)
